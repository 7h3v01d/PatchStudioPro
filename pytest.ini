[pytest]
# The package lives under src/, so put it on sys.path for test collection.
# Without this, `pytest src/tests` from the repo root fails to import patchstudio.
pythonpath = src
testpaths = src/tests
